model Plan {
  id        Int              @id @default(autoincrement())
  title     String
  createdAt DateTime         @default(now())
  updatedAt DateTime         @default(now())
  translations EntityTranslation[] @relation("PlanTranslations")
}