model Promo {
  id        Int          @id @default(autoincrement())
  code      String       @unique
  discount  Float
  createdAt DateTime     @default(now())
  updatedAt DateTime     @default(now())
  translations EntityTranslation[] @relation("PromoTranslations")
}